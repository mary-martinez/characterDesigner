export function makeStatsString(headCount, middleCount, bottomCount) {
    return `You clicked on the head ${headCount} times, on the middle ${middleCount} times, and on the bottom ${bottomCount} times. And everybody loves your character's catchphrases:`;
}